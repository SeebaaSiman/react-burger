import { SectionContentSettings } from "../../../ui/styles/settings-style";

const SecurityLogin = () => (
  <SectionContentSettings>
    <h2>Seguridad y Acceso</h2>
    <p>Administra tu contraseña, autenticación en dos pasos y opciones de seguridad.</p>
  </SectionContentSettings>
);

export default SecurityLogin;
